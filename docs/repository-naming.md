# Repository Naming

This repository follows the Kungfu `paper-*` convention.

```text
paper-episodes-to-primitives
```

`paper` identifies an independently versioned research publication. The suffix
names the paper's central movement from governed causal Episodes to candidate
Primitives. It does not imply that every Episode produces a Primitive or that
the repository implements an autonomous discovery runtime.
